@using Sandbox;
@using Sandbox.UI;
@inherits Panel
@namespace HNS

<div id="blind-screen" @ref="Panel"></div>

@code
{
    Panel Panel { get; set; }
    
    public void Toggle(bool toggle) => Panel.SetClass("visible", toggle);
}

@using Sandbox;
@using Sandbox.UI;
@inherits Panel
@namespace HNS

@code
{
	public override void Tick()
	{
		var player = Player.GetLocal();
		if (player == null) return;

		SetClass("visible", player.IsFrozen);
	}
}
